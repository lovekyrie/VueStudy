{
  "version": "0.2.0",
  "configurations": [{
      "type": "chrome",
      "request": "launch",
      "program": "${file}",
      "name": "Launch Chrome",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${file}"
    }
  ]
}